#ifndef RENDER_CONTROLLER_DEFS_H
#define RENDER_CONTROLLER_DEFS_H

typedef enum
{
  RENDER_CONTROLLER_STATUS_OK = 0U,
  RENDER_CONTROLLER_STATUS_BUSY,
  RENDER_CONTROLLER_STATUS_ERROR,
} render_controller_status;

#endif
